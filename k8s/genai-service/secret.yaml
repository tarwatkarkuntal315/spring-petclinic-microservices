apiVersion: v1
kind: Secret
metadata:
  name: genai-secrets
  namespace: petclinic
  labels:
    app.kubernetes.io/name: genai-service
    app.kubernetes.io/part-of: petclinic
type: Opaque
stringData:
  # IMPORTANT: Replace this placeholder with your actual key before applying.
  # Never commit a real API key to source control — use kubectl create secret
  # or a secrets manager (AWS Secrets Manager, Vault) in production.
  OPENAI_API_KEY: REPLACE_WITH_YOUR_OPENAI_API_KEY
