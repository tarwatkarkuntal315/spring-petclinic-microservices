apiVersion: v1
kind: Service
metadata:
  name: vets-service
  namespace: petclinic
  labels:
    app.kubernetes.io/name: vets-service
    app.kubernetes.io/part-of: petclinic
spec:
  type: ClusterIP
  selector:
    app: vets-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
