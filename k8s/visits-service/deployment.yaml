apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: petclinic
  labels:
    app.kubernetes.io/name: visits-service
    app.kubernetes.io/part-of: petclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
        app.kubernetes.io/name: visits-service
        app.kubernetes.io/part-of: petclinic
    spec:
      initContainers:
        - name: wait-for-dependencies
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until wget -qO- http://config-server:8888/actuator/health && \
                    wget -qO- http://discovery-server:8761/actuator/health; do
                echo "waiting for config-server and discovery-server..."; sleep 5
              done
      containers:
        - name: visits-service
          # Replace <ACCOUNT_ID> with your AWS account ID before applying.
          # ECR repo: petclinic/visits-service (infra/modules/ecr/main.tf)
          image: <ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/petclinic/visits-service:latest
          ports:
            - containerPort: 8082
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
